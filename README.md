# Solvencia de Biblioteca

## To run in local
- cd /client
- sudo npm install
- bower install
- grunt server

## To deploy in production 
- cd /client
- sudo npm install
- bower install
- grunt 
- copy only the /dist directory in server